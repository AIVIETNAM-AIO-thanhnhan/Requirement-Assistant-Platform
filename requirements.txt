# ─────────────────────────────────────────────
# Python 3.10 or 3.11 recommended
# Install: pip install -r requirements.txt
#
# LLM options (choose one):
#   Option A — Ollama (free, local, no API key needed) ← recommended for beginners
#   Option B — OpenAI (paid API, better accuracy)
# ─────────────────────────────────────────────

# ── Frontend ──────────────────────────────────
streamlit==1.58.0

# ── Document Parsing ──────────────────────────
pypdf==6.13.2           # PDF text extraction
python-docx==1.2.0      # DOCX text extraction

# ── RAG Framework ─────────────────────────────
langchain==1.3.7
langchain-ollama==1.1.0          # LangChain integration for Ollama  (Option A)
langchain-community==0.4.2       # Required by langchain-ollama
# langchain-openai==1.3.0        # LangChain integration for OpenAI  (Option B — uncomment if using OpenAI)

# ── LLM & Embeddings ──────────────────────────
# Option A: Ollama (install Ollama app separately — see setup notes below)
sentence-transformers==5.5.1     # Free local embeddings — works with both options

# Option B: OpenAI (uncomment if using OpenAI instead of Ollama)
# openai==2.41.1                 # OpenAI API (GPT-4o-mini + text-embedding-3-small)

# ── Vector Database ───────────────────────────
chromadb==1.5.9         # Local persistent vector store (recommended)
# faiss-cpu==1.9.0      # Alternative: FAISS (uncomment if using FAISS instead)

# ── Backend API (optional) ────────────────────
fastapi==0.136.3        # REST API layer (only needed if exposing a backend API)
uvicorn==0.49.0         # ASGI server for FastAPI

# ── Environment & Config ──────────────────────
python-dotenv==1.2.2    # Load API keys from .env file (still useful for config vars)

# ─────────────────────────────────────────────
# OLLAMA SETUP (Option A) — do this before running the app
#
# 1. Download and install Ollama: https://ollama.com/download
# 2. Pull a model (run in terminal):
#       ollama pull llama3.2        ← recommended: fast, good quality, ~2GB
#       ollama pull mistral         ← alternative: strong reasoning
#       ollama pull gemma3          ← alternative: lightweight, runs on low RAM
# 3. Ollama runs as a local server at http://localhost:11434 — keep it running
# 4. No API key needed. No internet required after the model is downloaded.
#
# Recommended model by hardware:
#   8GB  RAM  → llama3.2 (3B)   or gemma3 (4B)
#   16GB RAM  → llama3.2 (8B)   or mistral (7B)
#   32GB RAM  → llama3.1 (8B)   or mixtral (8x7B)
# ─────────────────────────────────────────────
